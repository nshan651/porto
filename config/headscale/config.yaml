server_url: https://hs.nshan651.duckdns.org:443

# Set this to 0.0.0.0:8080 when behind a reverse proxy.
listen_addr: 0.0.0.0:8080
metrics_listen_addr: 127.0.0.1:9090

grpc_listen_addr: 0.0.0.0:50443
grpc_allow_insecure: false

private_key_path: /var/lib/headscale/private.key

noise:
  private_key_path: /var/lib/headscale/noise_private.key

prefixes:
  v4: 100.64.0.0/10
  v6: fd7a:115c:a1e0::/48

derp:
  server:
    enabled: false
    # Region ID to use for the embedded DERP server.
    # The local DERP prevails if the region ID collides with other region ID coming from
    # the regular DERP config.
    region_id: 999

    # Region code and name are displayed in the Tailscale UI to identify a DERP region.
    region_code: "headscale"
    region_name: "Headscale Embedded DERP"

    stun_listen_addr: "0.0.0.0:3478"

    # Private key used to encrypt the traffic between headscale DERP and
    # Tailscale clients. A missing key will be automatically generated.
    private_key_path: /var/lib/headscale/derp_server_private.key
    automatically_add_embedded_derp_region: true

  # List of externally available DERP maps encoded in JSON
  urls:
    - https://controlplane.tailscale.com/derpmap/default

  paths: []

  auto_update_enabled: true

  # How often should we check for DERP updates?
  update_frequency: 24h

database:
  type: sqlite3
  sqlite:
    path: /var/lib/headscale/db.sqlite
    write_ahead_log: true


acme_url: https://acme-v02.api.letsencrypt.org/directory
acme_email: "admin@mail.com"

tls_letsencrypt_hostname: ""
tls_letsencrypt_cache_dir: /var/lib/headscale/cache
tls_letsencrypt_challenge_type: HTTP-01
tls_letsencrypt_listen: ":http"

tls_cert_path: ""
tls_key_path: ""

log:
  format: text
  level: info

dns:
  magic_dns: true
  base_domain: arpa
  override_local_dns: true
  nameservers:
    global:
      - 1.1.1.1
      - 8.8.8.8
  extra_records:
    - name: "syncthing.layerseven.duckdns.org"
      type: "A"
      value: "100.64.0.1"
    - name: "pass.layerseven.duckdns.org"
      type: "A"
      value: "100.64.0.1"
    - name: "nextcloud.layerseven.duckdns.org"
      type: "A"
      value: "100.64.0.1"
    - name: "rss.layerseven.duckdns.org"
      type: "A"
      value: "100.64.0.1"
    - name: "jellyfin.layerseven.duckdns.org"
      type: "A"
      value: "100.64.0.1"
    - name: "auth.layerseven.duckdns.org"
      type: "A"
      value: "100.64.0.1"

  search_domains: []

unix_socket: /var/run/headscale/headscale.sock
unix_socket_permission: "0770"

logtail:
  enabled: false

randomize_client_port: false
